.forge/
build/
dist/
logs/
node_modules/
*.local.json

# The Forge runtime is never committed. Probes require the pinned desktop
# distribution supplied out of band; see docs/observation-schema.md.
# Anchored to the repository root so a nested source directory such as
# spike/src/main/java/forge/ is never ignored by accident.
/forge/
/forge-*/
